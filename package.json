{
  "name": "hecto-drive",
  "scripts": {
    "build": "npm run build:ui && npm run build:app",
    "build:ui": "cd src/ui && npm run build",
    "build:app": "cd src/app && cargo xtask bundle hecto_drive --release"
  }
}
