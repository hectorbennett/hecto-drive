import { css } from "@emotion/react";

export default function Logo() {
  return (
    <div
      css={css`
        font-family: "Elphinstone";
        font-size: 2.2rem;
      `}
    >
      el-hectro-harmonix
    </div>
  );
}
